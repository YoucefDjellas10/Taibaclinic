<odoo>
    <record id="view_details_received_wizard_form" model="ir.ui.view">
        <field name="name">details.received.wizard.form</field>
        <field name="model">details.received.wizard</field>
        <field name="arch" type="xml">
            <form string="Cancel Appointment">
                <sheet>
                    <group>
                        <field name="patient_id" invisible="1" readonly="1"/>
                    </group>
                    <group>
                        <field name="name"/>
                        <field name="attachment_ids" widget="many2many_binary"/>
                    </group>
                </sheet>
                <footer>
                    <button
                            name="action_confirm_action"
                            string="Confirm"
                            type="object"
                            class="btn-danger"
                    />
                    <button
                            string="Discard"
                            class="btn-secondary"
                            special="cancel"
                    />
                </footer>
            </form>
        </field>
    </record>
</odoo>
